<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="QuickCode.Controls.QrCodeWifiControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QuickCode.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:QuickCode.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance vm:QrWifiDataViewModel, IsDesignTimeCreatable=False}"
    DataContextChanged="OnDataContextChanged">
    <Grid ColumnSpacing="10" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBox Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                 x:Uid="QrCodeWifiSsidBox"
                 Text="{Binding Ssid, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

        <ComboBox Grid.Row="1" Grid.Column="0"
                  x:Uid="QrCodeWifiEncryptionComboBox"
                  ItemsSource="{Binding Encryptions}"
                  SelectedItem="{Binding SelectedEncryption, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

        <PasswordBox Grid.Row="1" Grid.Column="1"
                     x:Uid="QrCodeWifiPasswordBox"
                     x:Name="PasswordBox"
                     PasswordChanged="OnPasswordChanged"/>

        <ToggleSwitch Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                      x:Uid="QrCodeWifiHiddenNetworkToggle"
                      IsOn="{Binding IsHidden, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
    </Grid>
</UserControl>
